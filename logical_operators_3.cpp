//Logical NOT Operator
#include <stdio.h> 
main ()
{
int x = 5;
printf (" The return value = %d \n", ! (x == 5));
printf (" The return value = %d \n", ! (x != 5));
printf (" The return value = %d \n", ! (x >= 3));
printf (" The return value = %d \n", ! (x < 3));
}
